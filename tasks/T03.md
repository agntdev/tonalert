# T03 — /help, unknown-command & error boundary

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TALRT

Implement `/help` (lists the commands), a friendly fallback for unknown commands, and a global error boundary so an unhandled error replies gracefully instead of crashing the bot. Append the relevant specs to `tests/specs.json`.

## Skills

**Skills:** telegram-bot-basics

Read `tasks/_skills/<name>.md` for each skill above before implementing.
