# T02 — /start command + main menu

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TALRT

Implement `/start`: a welcome message and the bot's main menu (inline keyboard) routing to its top-level features. Append the `/start` dialog spec to `tests/specs.json` and the command to `tests/commands.json`.

## Skills

**Skills:** telegram-bot-basics, telegram-bot-ui

Read `tasks/_skills/<name>.md` for each skill above before implementing.
